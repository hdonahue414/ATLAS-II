function cats(){return DATA.categories||[]}function denom(){return Object.values(weights).reduce((a,b)=>a+b,0)||1}function avg(a){return a.length?a.reduce((x,y)=>x+y,0)/a.length:0}function catValue(s,k){let a=s.scores?.[k]?.subvariables||[];return avg(a.map(x=>x.value||0))}function catConf(s,k){let a=s.scores?.[k]?.subvariables||[];return avg(a.map(x=>x.confidence??.5))}function score(s){let raw=0;cats().forEach(c=>raw+=catValue(s,c.key)*(weights[c.key]||0));return Math.round(raw/denom()*1000)}function confidence(s){let raw=0;cats().forEach(c=>raw+=catConf(s,c.key)*(weights[c.key]||0));return raw/denom()}function ranked(){return [...DATA.schools].map((s,i)=>({...s,_i:i,_score:score(s)})).sort((a,b)=>b._score-a._score)}function pct(v){return Math.round((v||0)*100)}function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}function bar(v){return `<div class="bar"><div class="fill" style="width:${Math.max(0,Math.min(100,Math.round(v)))}%"></div></div>`}function ring(v,color,small=false){return `<div class="ring ${small?'miniRing':''}" style="--p:${Math.max(0,Math.min(100,v))};--ringColor:${color}"><span>${Math.round(v)}</span></div>`}function pills(a){return(a||[]).map(x=>`<span class="pill">${esc(x)}</span>`).join(' ')}function pick(){return ranked().map(s=>`<button class="schoolBtn ${selected===s._i?'active':''}" onclick="selected=${s._i};render()">${s.name}</button>`).join('')}
