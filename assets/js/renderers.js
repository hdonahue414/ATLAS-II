function atlasContextButtonStyles(){if(document.getElementById('atlasContextButtonStyles'))return;let style=document.createElement('style');style.id='atlasContextButtonStyles';style.textContent='.atlasContextBtn,.toriCinematicHero .toriContextBtn{margin-top:14px!important;width:100%!important;display:flex!important;align-items:center!important;justify-content:center!important;gap:8px!important;border:1px solid rgba(157,231,215,.22)!important;border-radius:14px!important;background:linear-gradient(180deg,rgba(12,22,38,.62),rgba(5,9,20,.56))!important;backdrop-filter:blur(10px);box-shadow:0 10px 28px rgba(0,0,0,.20),inset 0 1px 0 rgba(255,255,255,.05)!important;padding:10px 12px!important;color:#e6edf8!important;font-size:.78rem!important;line-height:1!important;letter-spacing:.02em!important;cursor:pointer!important;transition:border-color .18s ease,background .18s ease,color .18s ease,transform .18s ease}.atlasContextBtn:before,.toriCinematicHero .toriContextBtn:before{content:"i";display:grid;place-items:center;width:17px;height:17px;border:1px solid rgba(157,231,215,.34);border-radius:50%;color:#9de7d7;font-size:.68rem;font-weight:800;line-height:1}.atlasContextBtn:hover,.toriCinematicHero .toriContextBtn:hover{border-color:rgba(157,231,215,.42)!important;background:linear-gradient(180deg,rgba(20,34,55,.74),rgba(8,15,28,.66))!important;color:#fff!important;transform:translateY(-1px)}.atlasContextBtn{max-width:260px}.toriCinematicHero .toriContextBtn{max-width:none!important}';document.head.appendChild(style)}
function atlasProgramDossierStyles(){if(document.getElementById('atlasProgramDossierStyles'))return;let style=document.createElement('style');style.id='atlasProgramDossierStyles';style.textContent='.programDossier{display:grid;gap:18px}.programDossierHero{position:relative;min-height:clamp(430px,48vw,590px);border:1px solid rgba(255,255,255,.12);border-radius:30px;overflow:hidden;background:#080d17;box-shadow:0 24px 80px rgba(0,0,0,.34)}.programDossierHero img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;opacity:.70;filter:saturate(.86) contrast(1.06)}.programDossierHero:before{content:"";position:absolute;inset:0;z-index:1;background:radial-gradient(circle at 22% 70%,rgba(3,7,14,.86),rgba(3,7,14,.50) 32%,transparent 62%),linear-gradient(90deg,rgba(3,7,14,.76),rgba(3,7,14,.30) 42%,rgba(3,7,14,.08) 72%,transparent)}.programDossierHero:after{content:"";position:absolute;inset:0;z-index:1;background:linear-gradient(180deg,rgba(0,0,0,.02),rgba(2,6,14,.12) 42%,rgba(2,6,14,.72))}.programDossierBody{position:absolute;inset:auto 0 0;z-index:2;padding:clamp(22px,4vw,38px);display:grid;gap:16px;max-width:980px;text-shadow:0 2px 18px rgba(0,0,0,.55)}.programDossierKicker{width:max-content;color:rgba(220,231,247,.66);font-size:.68rem;letter-spacing:.14em;text-transform:uppercase;background:rgba(5,9,20,.32);border:1px solid rgba(255,255,255,.08);border-radius:999px;padding:5px 8px;backdrop-filter:blur(8px)}.programDossierTitle{margin:0;color:var(--programAccent,#dce7f7);font-size:clamp(2rem,5vw,4.6rem);line-height:.94;letter-spacing:-.035em;text-shadow:0 4px 28px rgba(0,0,0,.72)}.programDossierPlace{color:#d6e1f1;font-size:.82rem;letter-spacing:.09em;text-transform:uppercase}.programDossierRead{max-width:760px;margin:0;color:#f4f8ff;font-size:clamp(1rem,1.65vw,1.22rem);line-height:1.48}.programDossierSignals{display:flex;gap:10px;flex-wrap:wrap;align-items:center}.programDossierSignal{display:flex;align-items:center;gap:8px;border:1px solid rgba(255,255,255,.14);border-radius:999px;background:rgba(5,9,20,.50);backdrop-filter:blur(10px);padding:7px 10px;color:#dce7f7;font-size:.76rem;box-shadow:inset 0 1px 0 rgba(255,255,255,.04)}.programDossierSignal b{font-size:.84rem;color:#fff}.programDossierNav{display:flex;gap:7px 10px;flex-wrap:wrap;align-items:center;margin-top:2px}.programDossierNav .schoolBtn{position:relative;background:rgba(5,9,20,.22)!important;border:1px solid rgba(255,255,255,.10)!important;border-radius:999px!important;color:rgba(220,231,247,.76)!important;padding:8px 11px 8px 22px!important;box-shadow:none!important}.programDossierNav .schoolBtn:before{content:"";position:absolute;left:9px;top:50%;width:5px;height:5px;border-radius:50%;transform:translateY(-50%);background:rgba(220,231,247,.28)}.programDossierNav .schoolBtn:hover{border-color:rgba(157,231,215,.26)!important;background:rgba(157,231,215,.07)!important;color:#f4f8ff!important}.programDossierNav .schoolBtn.active{background:rgba(157,231,215,.11)!important;border-color:rgba(157,231,215,.36)!important;color:#fff!important;font-weight:700}.programDossierNav .schoolBtn.active:before{background:var(--programAccent,#9de7d7);box-shadow:0 0 12px var(--programAccent,#9de7d7)}.programDossierGrid{display:grid;grid-template-columns:minmax(0,1.15fr) minmax(280px,.85fr);gap:18px;align-items:start}.programDossierPanel{background:rgba(11,18,32,.82);border:1px solid rgba(132,174,252,.12);border-radius:24px;padding:18px;box-shadow:0 18px 54px rgba(0,0,0,.22)}.programDossierPanel h3{margin:0 0 12px;font-size:1.05rem}.programDossierPanel p{color:#cbd7e8;line-height:1.55}.programFieldNotes{display:grid;gap:12px}.programFieldNote{border-left:1px solid rgba(157,231,215,.24);padding-left:12px;color:#d8e3f1;line-height:1.54}.programFieldNote b{display:block;margin-bottom:4px;color:#eef4ff;font-size:.78rem;letter-spacing:.08em;text-transform:uppercase}.programTemperament{display:grid;gap:12px}.programTemperamentRow{display:grid;grid-template-columns:minmax(0,1fr) 54px;gap:12px;align-items:center}.programTemperamentRow span{color:#dce7f7}.programTemperamentRow small{color:var(--muted);text-align:right}.programTemperamentRow .bar{grid-column:1/-1;margin:0;height:6px;background:rgba(132,174,252,.10)}.programSectionGrid{display:grid;grid-template-columns:repeat(auto-fit,minmax(250px,1fr));gap:14px}.programSection{background:rgba(8,14,27,.62);border:1px solid rgba(132,174,252,.10);border-radius:20px;padding:15px}.programSection h3{margin:0 0 8px;font-size:.98rem;color:#edf4ff}.programSection p{margin:0 0 11px;color:#b8c5d9;line-height:1.48}.programSubsignals{display:grid;gap:7px}.programSubsignal{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;align-items:center;color:#dce7f7;font-size:.83rem}.programSubsignal small{color:rgba(220,231,247,.58)}.programTracePanel .metric{background:rgba(8,14,27,.50);border-color:rgba(132,174,252,.10)}@media(max-width:900px){.programDossierGrid{grid-template-columns:1fr}.programDossierHero{min-height:520px}.programDossierTitle{font-size:clamp(2.1rem,12vw,3.7rem)}.programDossierRead{max-width:100%}.programDossierNav .schoolBtn{padding-top:9px!important;padding-bottom:9px!important}}';document.head.appendChild(style)}
function render(){atlasContextButtonStyles();atlasProgramDossierStyles();document.getElementById('panel').innerHTML=VIEWS[view]();if(view==='visuals')setTimeout(drawCharts,50)}
const VIEWS={overview(){return `<div class="heroGrid">${ranked().map((s,i)=>schoolCard(s,i)).join('')}</div>`},compare(){return compareView()},presets(){return presetView()},programs(){return programDossierView(DATA.schools[selected])},evidence(){return evidenceView()},fit(){return page('Fit',philosophyBlock()+dailyLifeBlock()+failureBlock()+identityBlock()+environmentBlock()+mentorDependencyBlock())},decision(){return page('Decision',forecastBlock()+futureSelfBlock()+attractionBlock()+regretBlock()+antiBlock())},visuals(){return page('Visuals',chartCards()+heatmap()+confidenceMatrix()+portfolioBlock())},location(){return page('Location',locationCards()+ecosystemMap()+dailyCostBlock())},development(){return page('Development',applicationBlock()+portfolioGaps()+calcView('Thesis feasibility',DATA.operational_modules?.thesis_simulator?.inputs||{},'Pamunkey remains exploratory/non-core.'))}};
function schoolCard(s,i){let c=s.brand_colors||{primary:'#84aefc',accent:'#9de7d7'};let loc=s.location_intelligence||{};return `<div class="schoolHero" onclick="selected=${s._i};setView('programs')" style="--school:${c.primary}"><img src="${s.visual_identity?.photo_local||''}"><div class="heroBody"><div style="display:flex;justify-content:space-between;gap:12px;align-items:end"><div><h2 style="color:${c.accent}">${s.name}</h2><div class="desc">${loc.city||s.location?.city||''}</div></div>${ring(score(s)/10,c.primary)}</div><div style="margin-top:10px">${pills((loc.points_of_interest||[]).slice(0,3))}</div><button class="atlasContextBtn harperContextBtn" onclick="event.preventDefault();event.stopPropagation();if(typeof atlasOpenToriContext==='function')atlasOpenToriContext(${s._i})">Environmental context</button></div></div>`}
function programDossierView(s){let c=s.brand_colors||{primary:'#84aefc',accent:'#9de7d7'},loc=s.location_intelligence||s.location||{},city=loc.city||s.location?.city||'',ecosystem=(s.documentary_ecosystem||[]).slice(0,4);return `<div class="programDossier" style="--programAccent:${c.accent};--programPrimary:${c.primary}"><section class="programDossierHero"><img src="${s.visual_identity?.photo_local||''}" alt=""><div class="programDossierBody"><div class="programDossierKicker">Institutional dossier</div><h2 class="programDossierTitle">${esc(s.name)}</h2><div class="programDossierPlace">${esc(city)}</div><p class="programDossierRead">${esc(programDossierRead(s))}</p><div class="programDossierSignals"><span class="programDossierSignal"><b>${score(s)}</b> program fit</span><span class="programDossierSignal"><b>${Math.round(confidence(s)*100)}%</b> evidence confidence</span><span class="programDossierSignal">${evidenceBadge(s)}</span></div><div class="programDossierNav">${pick()}</div></div></section><div class="programDossierGrid"><section class="programDossierPanel"><h3>Interpretive field notes</h3><div class="programFieldNotes">${programFieldNotes(s).map(x=>`<div class="programFieldNote"><b>${esc(x[0])}</b>${esc(x[1])}</div>`).join('')}</div></section><aside class="programDossierPanel"><h3>Institutional temperament</h3><div class="programTemperament">${programTemperamentRows(s)}</div></aside></div>${ecosystem.length?`<section class="programDossierPanel"><h3>Documentary-world positioning</h3>${pills(ecosystem)}</section>`:''}<section class="programSectionGrid">${programCategorySections(s)}</section><section class="programDossierPanel programTracePanel"><h3>Source trace</h3><div class="grid">${['verified','inferred','speculative','unresolved'].map(k=>`<div class="metric"><div class="k">${k}</div>${pills(s.source_trace?.[k]||[])}</div>`).join('')}</div></section></div>`}
function programDossierCopy(text){let v=String(text||'').replace(/\s+/g,' ').trim();if(!v)return '';let translations=[[/Can improve if Sama responds\.?/i,'Documentary mentorship outlook remains promising but unresolved pending direct faculty confirmation.'],[/Biggest upward revision from student\/faculty intel\.?/i,'Recent student and faculty signals suggest stronger fit than earlier evidence indicated.'],[/Narrowness is not heavily penalized because the user does not want expanded-media drift\.?/i,'A focused documentary structure may support depth rather than drift for this profile.'],[/Strongest structurally relational evidence\. Recent reschedule now interpreted as faculty overload rather than disengagement\.?/i,'Strong relational structure remains the clearest signal, with recent scheduling friction reading more like faculty overload than lack of interest.'],[/Recent reschedule now interpreted as faculty overload rather than disengagement\.?/i,'Recent scheduling friction reads more like faculty overload than lack of interest.'],[/Pamunkey remains exploratory\/non-core\.?/i,'The Pamunkey project remains exploratory rather than a core fit driver.']];translations.forEach(([re,out])=>{v=v.replace(re,out)});return v}
function programDossierRead(s){return programDossierCopy(s.scores?.documentary?.notes||s.ordinary_tuesday||s.identity_preservation_analysis?.summary||s.location?.energy_profile||'Program interpretation remains open as evidence accumulates.')}
function programFieldNotes(s){let items=[['Curricular rhythm',programDossierCopy(s.scores?.documentary?.notes||s.scores?.mentorship?.notes||'Curricular temperament requires closer reading.')],['Filmmaker-life implication',programDossierCopy(s.ordinary_tuesday||s.comparative_future_biography?.possible_regret||'Daily practice implications remain unresolved.')],['Sustainability pressure',programDossierCopy(s.failure_modes?.financial_stress?.notes||s.daily_cost_friction?.notes||s.identity_preservation_analysis?.summary||'Sustainability pressure should be read through funding, pace, and relationship support.')]];return items.filter(x=>x[1])}
function programTemperamentRows(s){return cats().map(cat=>({cat,value:catValue(s,cat.key)})).sort((a,b)=>b.value-a.value).slice(0,5).map(x=>`<div class="programTemperamentRow"><span>${esc(x.cat.label||x.cat.key)}</span><small>${Math.round(x.value*100)}</small>${bar(x.value*100)}</div>`).join('')}
function programCategorySections(s){return cats().map(cat=>{let record=s.scores?.[cat.key]||{},subs=(record.subvariables||[]).slice(0,3);return `<article class="programSection"><h3>${esc(cat.label||cat.key)}</h3><p>${esc(programDossierCopy(record.notes||'Evidence is present but still interpretively thin.'))}</p><div class="programSubsignals">${subs.map(x=>`<div class="programSubsignal"><span>${esc(x.name)}</span><small>${Math.round((x.value||0)*100)}</small></div>`).join('')}</div></article>`}).join('')}
function evidenceBadge(s){let st=s.evidence_state||'inferred';let cls=st==='corroborated'?'verified':st==='partial'?'partial':'inferred';return `<span class="evidenceBadge ${cls}">${st}</span>`}function page(t,b){return `<div class="card"><h3>${t}</h3></div>${b}`}function hint(t){return `<span class="hint" tabindex="0">i<span>${esc(t)}</span></span>`}
function compareView(){let opts=v=>ranked().map(s=>`<option value="${s._i}" ${v===s._i?'selected':''}>${s.name}</option>`).join('');let a=DATA.schools[compareA],b=DATA.schools[compareB];return `<div class="card"><select onchange="compareA=+this.value;render()">${opts(compareA)}</select><select onchange="compareB=+this.value;render()">${opts(compareB)}</select></div><div class="compareGrid">${compareCard(a)}${compareCard(b)}</div>${tableCard('Category comparison','<tr><th>Category</th><th>'+a.name+'</th><th>'+b.name+'</th></tr>'+cats().map(c=>`<tr><td>${c.key}</td><td>${Math.round(catValue(a,c.key)*100)}${bar(catValue(a,c.key)*100)}</td><td>${Math.round(catValue(b,c.key)*100)}${bar(catValue(b,c.key)*100)}</td></tr>`).join(''))}`}
function compareCard(s){return `<div class="card" style="border-color:${s.brand_colors?.primary}"><h2>${s.name}</h2><div class="grid"><div>${ring(score(s)/10,s.brand_colors?.primary)}<p class="desc">Score ${hint('Overall fit under the active weighting.')}</p></div><div>${ring(confidence(s)*100,s.brand_colors?.accent)}<p class="desc">Confidence ${hint('Evidence strength behind the comparison.')}</p></div></div><p>${esc(s.ordinary_tuesday||'')}</p>${pills(s.anti_delusion_checks)}</div>`}
function presetView(){let ps=Object.keys(DATA.decision_presets_v23||{});return `<div class="card">${['Baseline',...ps].map(p=>`<button class="schoolBtn ${preset===p?'active':''}" onclick="applyPreset('${p}')">${p}</button>`).join('')}</div><div class="heroGrid">${ranked().map((s,i)=>schoolCard(s,i)).join('')}</div>`}function applyPreset(p){preset=p;weights={...(DATA.scenario_presets?.baseline?.weights||Object.fromEntries(cats().map(c=>[c.key,c.weight])))};if(p!=='Baseline'){let mult=DATA.decision_presets_v23[p]||{};Object.keys(weights).forEach(k=>{if(mult[k])weights[k]*=mult[k]})}render()}
function categoryBlocks(s){return cats().map(cat=>`<div class="card"><h3>${cat.label||cat.key}</h3><div class="grid">${(s.scores?.[cat.key]?.subvariables||[]).map(x=>`<div class="metric"><div class="k">${esc(x.name)}</div><div class="num">${Math.round((x.value||0)*100)}</div>${bar((x.value||0)*100)}</div>`).join('')}</div></div>`).join('')}function sourceTrace(s){let tr=s.source_trace||{};return `<div class="card"><h3>Source trace</h3><div class="grid">${['verified','inferred','speculative','unresolved'].map(k=>`<div class="metric"><div class="k">${k}</div>${pills(tr[k]||[])}</div>`).join('')}</div></div>`}
function evidenceView(){return `<div class="grid">${ranked().map(s=>`<div class="card"><h3>${s.name} ${evidenceBadge(s)}</h3>${sourceTrace(s)}</div>`).join('')}</div>`+relationshipBlock()+outcomesBlock()+testimonyBlock()}
function relationshipBlock(){let rows=DATA.schools.flatMap(s=>(s.relationship_tracker||[]).map(x=>`<tr><td><b>${s.name}</b></td><td>${esc(x.person)}</td><td>${esc(x.role)}</td><td>${esc(x.tone)}</td><td>${esc(x.residue)}</td></tr>`)).join('');return tableCard('Relationship evidence','<tr><th>School</th><th>Person</th><th>Role</th><th>Tone</th><th>Residue</th></tr>'+rows)}function outcomesBlock(){return `<div class="grid">${ranked().map(s=>`<div class="card"><h3>${s.name}</h3><p class="desc">${esc(s.alumni_current_student_outcomes?.outcome_pattern||'')}</p></div>`).join('')}</div>`}function testimonyBlock(){return `<div class="grid">${ranked().map(s=>`<div class="card"><h3>${s.name}</h3>${(s.public_testimony||[]).slice(0,2).map(p=>`<p>${esc(p.summary)}</p>`).join('')||'<p class="desc">No testimony.</p>'}</div>`).join('')}</div>`}
function philosophyBlock(){let axes=['observational','relational','experimental','academic','collaborative','process_oriented'];return tableCard('Documentary fingerprint','<tr><th>School</th>'+axes.map(a=>`<th>${a}</th>`).join('')+'</tr>'+ranked().map(s=>`<tr><td><b>${s.name}</b></td>${axes.map(a=>`<td>${pct(s.documentary_philosophy_map?.[a])}${bar(pct(s.documentary_philosophy_map?.[a]))}</td>`).join('')}</tr>`).join(''))}
function dailyLifeBlock(){return `<div class="grid">${ranked().map(s=>`<div class="card"><h3>${s.name}</h3><p>${esc(s.ordinary_tuesday||'')}</p>${pills(s.advisor_archetypes)}</div>`).join('')}</div>`}function failureBlock(){return tableCard('Stress resilience','<tr><th>School</th><th>Low energy</th><th>Financial</th><th>Creative block</th></tr>'+ranked().map(s=>`<tr><td><b>${s.name}</b></td><td>${pct(s.failure_modes?.low_energy_period?.resilience)}${bar(pct(s.failure_modes?.low_energy_period?.resilience))}</td><td>${pct(s.failure_modes?.financial_stress?.resilience)}${bar(pct(s.failure_modes?.financial_stress?.resilience))}</td><td>${pct(s.failure_modes?.creative_block?.resilience)}${bar(pct(s.failure_modes?.creative_block?.resilience))}</td></tr>`).join(''))}
function identityBlock(){return `<div class="grid">${ranked().map(s=>`<div class="card"><h3>${s.name}</h3>${ring(pct(s.identity_preservation_analysis?.score),s.brand_colors?.primary)}<p>${esc(s.identity_preservation_analysis?.summary||'')}</p></div>`).join('')}</div>`}
function environmentBlock(){return tableCard('Environmental rhythm','<tr><th>School</th><th>Pace</th><th>Sensory load</th><th>Seasonal risk</th><th>Routine</th></tr>'+ranked().map(s=>`<tr><td><b>${s.name}</b></td><td>${pct(s.environmental_rhythm?.pace)}${bar(pct(s.environmental_rhythm?.pace))}</td><td>${pct(s.environmental_rhythm?.sensory_load)}${bar(pct(s.environmental_rhythm?.sensory_load))}</td><td>${pct(s.environmental_rhythm?.seasonal_risk)}${bar(pct(s.environmental_rhythm?.seasonal_risk))}</td><td>${pct(s.environmental_rhythm?.routine_feasibility)}${bar(pct(s.environmental_rhythm?.routine_feasibility))}</td></tr>`).join(''))}
function mentorDependencyBlock(){return tableCard('Mentorship dependency','<tr><th>School</th><th>Centrality</th><th>Redundancy</th><th>Succession risk</th></tr>'+ranked().map(s=>`<tr><td><b>${s.name}</b></td><td>${pct(s.mentorship_dependency_visual?.centrality)}${bar(pct(s.mentorship_dependency_visual?.centrality))}</td><td>${pct(s.mentorship_dependency_visual?.redundancy)}${bar(pct(s.mentorship_dependency_visual?.redundancy))}</td><td>${pct(s.mentorship_dependency_visual?.succession_risk)}${bar(pct(s.mentorship_dependency_visual?.succession_risk))}</td></tr>`).join(''))}
function forecastBlock(){return `<div class="grid">${ranked().map(s=>`<div class="card"><h3>${s.name}</h3><p>${esc(s.comparative_future_biography?.['10_year']||'')}</p><p class="desc">${esc(s.comparative_future_biography?.possible_regret||'')}</p></div>`).join('')}</div>`}function futureSelfBlock(){let labels=['creative_self','teaching_self','thriving_self','exhausted_self'];return `<div class="grid">${ranked().map(s=>`<div class="card"><h3>${s.name}</h3>${labels.map(l=>`<div class="metric"><div class="k">${l.replaceAll('_',' ')}</div><p>${esc(s.future_self_cards?.[l]||'')}</p></div>`).join('')}</div>`).join('')}</div>`}
function attractionBlock(){let s=DATA.schools[selected];return `<div class="card">${pick()}</div>`+objCards('Attraction: '+s.name,s.operational_scores?.attraction_decomposition||{})}function regretBlock(){let s=DATA.schools[selected];return objCards('Regret: '+s.name,s.operational_scores?.regret_minimization||{})}function antiBlock(){return `<div class="grid">${ranked().map(s=>`<div class="card"><h3>${s.name}</h3>${pills(s.anti_delusion_checks)}</div>`).join('')}</div>`}
function chartCards(){return `<div class="vizGrid"><div class="card"><h3>Scores ${hint('Ranked fit, shaped by the active weights.')}</h3><canvas id="scoreChart"></canvas></div><div class="card"><h3>Confidence ${hint('How much evidence supports each score.')}</h3><canvas id="confidenceChart"></canvas></div><div class="card"><h3>Identity ${hint('Estimated preservation of creative and personal coherence.')}</h3><canvas id="identityChart"></canvas></div><div class="card"><h3>Fit landscape ${hint('Structural position across the model axes.')}</h3><canvas id="landscapeChart"></canvas></div></div>`}function heatmap(){return tableCard('Category heatmap '+hint('Category-level forces behind the total score.'),'<tr><th>School</th>'+cats().map(c=>`<th>${c.key}</th>`).join('')+'</tr>'+ranked().map(s=>`<tr><td><b>${s.name}</b></td>${cats().map(c=>`<td>${Math.round(catValue(s,c.key)*100)}${bar(catValue(s,c.key)*100)}</td>`).join('')}</tr>`).join(''))}function confidenceMatrix(){return tableCard('Confidence matrix '+hint('Where evidence is strong, thin, or inferred.'),'<tr><th>School</th>'+cats().map(c=>`<th>${c.key}</th>`).join('')+'</tr>'+ranked().map(s=>`<tr><td><b>${s.name}</b></td>${cats().map(c=>`<td>${Math.round(catConf(s,c.key)*100)}%${bar(catConf(s,c.key)*100)}</td>`).join('')}</tr>`).join(''))}function portfolioBlock(){let ps=DATA.portfolio_projects?.map(p=>p.title)||[];return tableCard('Portfolio resonance '+hint('How strongly each program matches the existing work.'),'<tr><th>School</th>'+ps.map(p=>`<th>${p}</th>`).join('')+'</tr>'+ranked().map(s=>`<tr><td><b>${s.name}</b></td>${ps.map(p=>`<td>${pct(s.portfolio_resonance?.[p])}${bar(pct(s.portfolio_resonance?.[p]))}</td>`).join('')}</tr>`).join(''))}
function locationCards(){return ranked().map(s=>{let l=s.location_intelligence||{};return `<div class="card locCard" style="--school:${s.brand_colors?.primary||'#84aefc'}"><img src="${s.visual_identity?.photo_local||''}"><div><h2 style="margin-top:0;color:${s.brand_colors?.accent||'#9de7d7'}">${s.name}</h2><p class="desc">${esc(l.city||'')}</p><p>${esc(l.notes||'')}</p><h3>Neighborhoods</h3>${pills(l.neighborhoods)}<h3>Points of interest</h3>${pills(l.points_of_interest)}<h3>LGBTQ resources</h3><div class="linkGrid">${(l.lgbtq_resources||[]).map(x=>`<a class="linkCard" href="${x.url}" target="_blank">☂ ${esc(x.name)}</a>`).join('')}</div><h3>DSA / left infrastructure</h3><div class="linkGrid">${(l.dsa_chapters||[]).map(x=>`<a class="linkCard" href="${x.url}" target="_blank">☭ ${esc(x.name)}</a>`).join('')}</div></div></div>`}).join('')}
function ecosystemMap(){return `<div class="grid">${ranked().map(s=>`<div class="card"><h3>${s.name}</h3>${pills(s.documentary_ecosystem)}</div>`).join('')}</div>`}function dailyCostBlock(){return tableCard('Daily cost friction','<tr><th>School</th><th>Rent</th><th>Food/transit</th><th>Healthcare</th><th>Funding buffer</th><th>Overall</th></tr>'+ranked().map(s=>`<tr><td><b>${s.name}</b></td><td>${pct(s.daily_cost_friction?.rent_pressure)}${bar(pct(s.daily_cost_friction?.rent_pressure))}</td><td>${pct(s.daily_cost_friction?.food_transit_pressure)}${bar(pct(s.daily_cost_friction?.food_transit_pressure))}</td><td>${pct(s.daily_cost_friction?.healthcare_friction)}${bar(pct(s.daily_cost_friction?.healthcare_friction))}</td><td>${pct(s.daily_cost_friction?.funding_buffer)}${bar(pct(s.daily_cost_friction?.funding_buffer))}</td><td>${pct(s.daily_cost_friction?.overall)}${bar(pct(s.daily_cost_friction?.overall))}</td></tr>`).join(''))}
function applicationBlock(){let s=DATA.schools[selected];return `<div class="card">${pick()}</div>`+objCards('Application alignment: '+s.name,s.operational_scores?.application_alignment||{})}function portfolioGaps(){return objCards('Portfolio gaps',DATA.operational_modules?.portfolio_gap_analysis?.gaps||{})}function calcView(title,obj,note){let id='calcbox';return `<div class="card" id="${id}"><h3>${title}</h3>${Object.entries(obj).map(([k,v])=>`<div class="metric"><div class="k">${k.replaceAll('_',' ')}</div><input type="range" min="0" max="10" value="${v}" oninput="document.getElementById('v_${k}').textContent=this.value;calc('${id}')"><b id="v_${k}">${v}</b></div>`).join('')}<div class="metric"><div class="k">Average</div><div id="${id}_score" class="num">—</div></div><button class="btn primary" onclick="calc('${id}')">Calculate</button><p class="desc">${note}</p></div>`}function calc(id){let vals=[...document.querySelectorAll(`#${id} input`)].map(x=>+x.value);document.getElementById(`${id}_score`).textContent=Math.round(avg(vals)*10)+'%'}
function tableCard(title,body){return `<div class="card"><h3>${title}</h3><div class="tableWrap"><table>${body}</table></div></div>`}function objCards(title,o){return `<div class="card"><h3>${title}</h3><div class="grid">${Object.entries(o).sort((a,b)=>b[1]-a[1]).map(([k,v])=>`<div class="metric"><div class="k">${k.replaceAll('_',' ')}</div><div class="visualRow">${ring(v*10,'#84aefc',true)}<div class="num">${v}/10</div></div></div>`).join('')}</div></div>`}
